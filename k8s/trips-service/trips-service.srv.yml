apiVersion: v1
kind: Service
metadata:
  name: trips-service-srv
  labels:
    app: trips-service
  namespace: staging-ns
spec:
  type: ClusterIP
  selector: 
    app: trips-service
  ports:
    - name: http
      port: 80
      targetPort: 80