apiVersion: apps/v1
kind: Deployment
metadata:
  name: trips-service-dp
  labels:
    app: trips-service
  namespace: staging-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trips-service
  template:
    metadata:
      labels:
        app: trips-service
    spec:
      containers:
        - name: trips-service-php
          image: docker.io/rvanappeldorn/trips-service:latest
          imagePullPolicy: Never
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: DB_USER
              valueFrom:
                secretKeyRef: { name: trips-db-credentials, key: user }
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef: { name: trips-db-credentials, key: password }
            - name: DATABASE_URL
              value: mariadb+pymysql://$(DB_USER):$(DB_PASSWORD)@trips-service-db-srv.$(POD_NAMESPACE).svc.cluster.local:3306/accounts?charset=utf8mb4
          ports:
            - containerPort: 9000
        - name: trip-service-nginx
          image: nginx:1.27-alpine
          ports: 
            - containerPort: 80 
          volumeMounts:
            - name: webroot
              mountPath: /var/www/html:ro
            - name: nginx
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
          readinessProbe:
            tcpSocket: { port: 9000 }
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits:   { cpu: "200m", memory: "256Mi" }
      volumes:
        - name: webroot
          emptyDir: {}
        - name: nginx
          configMap:
            name: trips-service-nginx-conf